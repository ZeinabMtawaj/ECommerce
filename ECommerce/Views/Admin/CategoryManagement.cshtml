
@{
    ViewData["Title"] = "CategoryManagement";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
@model IEnumerable<CategoryViewModel> 




<h1>Category Management</h1>


@if(Model != null && Model.Count() > 0)
{
   
}
@*else{
    <p> No Categories Yet</p>
}*@

<div>
	<!-- Button -->
	<div class="d-flex">
		<button class="btn btn-primary btn-round mr-1 mb-3" data-toggle="modal" data-target="#addRowModal">
			<i class="fa fa-plus"></i>
			Create
		</button>
	</div>



	<!-- Table -->
	<div class="table-responsive">
		<table id="add-row" class="display table table-striped table-hover" cellspacing="0" width="100%">
			<thead>
				 <tr>
				<th>Subscriber ID</th>
				<th>Install Location</th>
				<th>Subscriber Name</th>
				<th>some data</th>
				<th> Action</th>
			</tr>
			</thead>
			<tfoot>
				<tr>
			</tr>
			</tfoot>
			<tbody>
				<tr>
				<td>Subscriber ID</td>
				<td>Install Location</td>
				<td>Subscriber Name</td>
				<td>some data</td>
				<td>
					<div class="form-button-action">
						<button type="button" data-toggle="tooltip" data-placement="top" title="Edit" class="btn btn-link btn-simple-primary btn-lg" data-original-title="Edit Task">
							<i class="fa fa-edit"></i>
						</button>
						<button type="button" data-toggle="tooltip" data-placement="top" title="Remove" class="btn btn-link btn-simple-danger" data-original-title="Remove">
							<i class="fa fa-times"></i>
						</button>
					</div>
				</td>


			</tr>
			</tbody>
		</table>
	</div>

	<script>
		$('#add-row').DataTable({
			"pageLength": 5,
		});
	

	</script>

</div>